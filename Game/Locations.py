def import_ships():
    import Set_Up_Ships
    return Set_Up_Ships


def put_in_ship_data(ship_letter, ship_num, ship_name, num_of_coordinates):
    pass

Files = [
    A_file = ["Nothing"] * 10
    B_file = ["Nothing"] * 10
    C_file = ["Nothing"] * 10
    D_file = ["Nothing"] * 10
    E_file = ["Nothing"] * 10
    F_file = ["Nothing"] * 10
    G_file = ["Nothing"] * 10
    H_file = ["Nothing"] * 10
    I_file = ["Nothing"] * 10
    J_file = ["Nothing"] * 10
]
